<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.name %>'s Dashboard</title>
    
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
        }
        .sidebar {
            width: 250px;
            height: 100vh;
            background-color: #d32f2f;
            color: white;
            position: fixed;
            padding-top: 20px;
        }
        .sidebar h2 {
            text-align: center;
        }
        .sidebar ul {
            list-style: none;
            padding: 0;
        }
        .sidebar ul li {
            padding: 15px;
            text-align: center;
        }
        .sidebar ul li a {
            color: white;
            text-decoration: none;
            font-weight: bold;
            display: block;
        }
        .sidebar ul li:hover {
            background: #b71c1c;
        }
        .main-content {
            margin-left: 260px;
            padding: 20px;
        }
        .card {
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        input, select, button {
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
        }
        button {
            background-color: #d32f2f;
            color: white;
            border: none;
            cursor: pointer;
        }
        button:hover {
            background-color: #b71c1c;
        }
        .profile-image {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            display: block;
            margin: 10px auto;
            border: 3px solid #d32f2f;
        }
        .profile-info {
            text-align: center;
            font-size: 18px;
        }
    </style>
</head>
<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <h2>Bloodlink.com</h2>
        <ul>
            <li><a href="/donor/register">Donor Registration Form</a></li>
            <li><a href="/profile">Profile</a></li>
            <li><a href="/edit-profile">Edit Profile</a></li>
            <li><a href="/logout">Logout</a></li>
        </ul>
    </div>
    
    <!-- Main Content -->
    <div class="main-content">
        <section id="profile" class="card">
            <h2>Profile</h2>
            <div class="profile-info">
                <img src="<%= user.profileImage || '/default-profile.jpg' %>" alt="Profile Image" class="profile-image">
                <p><strong>Name:</strong> <span id="name"><%= user.name %></span></p>
                <p><strong>Blood Group:</strong> <span id="blood-group"><%= user.bloodGroup %></span></p>
                <p><strong>Email:</strong> <span id="email"><%= user.email %></span></p>
                <p><strong>Phone:</strong> <span id="phone"><%= user.phone %></span></p>
            </div>
        </section>
    </div>

</body>
</html>
